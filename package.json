{
  "name": "luna",
  "version": "1.0.0",
  "description": "An modular decorator based backend framework",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "repository": "https://github.com/ariusxi/luna",
  "author": "Alef Felix",
  "license": "MIT",
  "scripts": {
    "start:dev": "ts-node tests/main.ts",
    "build": "yarn workspace @lunafw/core build && yarn workspace @lunafw/common build && yarn workspace @lunafw/config build && yarn workspace @lunafw/platform-express build && yarn workspace @lunafw/platform-ws build && yarn workspace @lunafw/platform-grpc build && yarn workspace @lunafw/testing build && yarn workspace @lunafw/swagger build",
    "test": "yarn workspaces run test",
    "test:coverage": "yarn workspaces run test:coverage",
    "quality": "artie run .",
    "quality:baseline": "artie run . --save-baseline"
  },
  "devDependencies": {
    "artie-lens": "^1.18.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/github": "^12.0.8",
    "@semantic-release/npm": "^13.1.5",
    "@semantic-release/release-notes-generator": "^14.1.1",
    "@types/node": "^26.0.1",
    "semantic-release": "^25.0.5",
    "ts-node": "^10.9.2",
    "tslib": "^2.8.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "reflect-metadata": "^0.2.2",
    "semantic-release-monorepo": "^8.0.2"
  }
}
